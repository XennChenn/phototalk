// $('.like_post').bind('ajax:success', function(){
// 	$(this).parent().parent().find('.vote_count').html('<%= escape_javascript photo.votes_for.size.to_s %>');
// 	$(this).closest('.like_post').hide();
// 	$(this).closest('.votes').html('<%= link_to unlike_post_path(photo), remote: true, method: :get, class: "unlike_post" %>');
// });

// var like_post = $('.like_post');
// like_post.parent().parent().find('.vote_count').html('<%= escape_javascript photo.get_upvotes.size.to_s %>');
// like_post.closest('.like_post').hide();
// like_post.closest('.votes').html('<%= link_to unlike_photo_path(photo), remote: true, method: :get, class: "unlike_post" %>');

$('.like_post').bind('ajax:success', function(){
	$('.vote_count-<%= photo.id %>').html('<%= escape_javascript (render partial: upvote) %>');

});